import React from "react"

export default function Button(){
    return(
        <div className="buttons">
            <button>Email</button>
            <button>LinkedIn</button>            
        </div>
    )
}